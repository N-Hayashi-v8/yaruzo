# DESIGN.md

原始人口調。設計図。実装前 必読。

## 0. 一行

**次の1個だけ見せる。タイマー回す。それだけ。**

アプリ名 **やるぞ！**（リポジトリ `yaruzo`）。名前 = 起動の合図。
「やるぞ！」と口に出す瞬間に立ち上げるものとして作る。

## 1. 根拠 → 機能 対応表

各機能は研究上の根拠を持つ。根拠なき機能 追加禁止。

- **興味ベース神経系（INCUP）** → 重要度ソート廃止。刺激度ソート採用
- **開始時ドーパミン不足（Volkow 2009, 側坐核/中脳/尾状核）** → 開始摩擦をゼロに。起動 = 即 タスク表示
- **時間盲** → 経過時間を常時表示（カウントアップ）。時間を可視物体化。
  終わりは決めない（集中が切れるまでやる）。カウントダウンは 0 = 時間切れ = 失敗感を作る
- **巨大タスクで凍結** → 分解ボタン。1タップで 3ステップ
- **低覚醒（hypoarousal）** → 眠いボタン。刺激度高い順に切替
- **睡眠相後退（DSPS 有病率 26〜78%）** → 起床時刻固定 + 起床後 光 の1タップ記録
- **新規性減衰** → 報酬をランダム化。UI に微変動
- **ストリーク恥スパイラル** → 累積型カウンタのみ。連続日数 表示しない
- **配分を計画すると未達が生まれる** → 1日の時間割を作らない。**比率を決めない**。
  出るのは 古い順の先頭 1 件（下の 1.5章）
- **毎日おなじ文字を打つのは 摩擦** → 何度もやることは **定番** に登録。次から 1タップで生える

## 1.5. 定番

何度もやること（歯磨き・皿洗い・ストレッチ・日報）を **雛形** として登録しておく。
追加画面で 1タップ → タスクが生える。打ち直さない。

- 持つのは **タイトルだけ**（`lib/types.ts` の `Preset`）。
  登録時に 見積・刺激度を決めさせない = 入力項目を増やさない
- 並びは **最近使った順**。よく使うものが 勝手に前に来る
- 押し間違えても痛くないよう 定番の削除は 確認なし。もう一度打てば戻る
- **タグ・分類・フォルダは付けない**（7章）。定番が増えて探せなくなってから考える

**分類軸を持たない。** 以前は「3本の柱」（固定ラベル + 柱ごとの重み付き抽選）を持っていたが、
柱を重みで引いても 柱の中は最古 1 件 固定だったので、実際に出る組み合わせは
柱の数（最大 4 通り）しかなく、ランダムとして働いていなかった。
比率を数字で持つのは 目標を持つのと同じ。持たない。

時間割・曜日固定・「今日は◯分」は **作らない**。全部 未達の生成装置。

## 2. データ形状

`lib/types.ts` に定義。ここが唯一の真実。

```ts
export type Task = {
  id: string
  title: string
  estimateMin: number          // 目安分。バーが満ちる位置。超えても止まらない
  stimulation: 1 | 2 | 3       // 刺激度。1=退屈 3=面白い
  parentId: string | null      // 分解で生えた子タスク
  createdAt: number            // epoch ms
  completedAt: number | null   // null = 未完了
}

export type Preset = {         // 何度もやることの雛形（1.5章）
  id: string
  title: string                // 持つのはタイトルだけ
  lastUsedAt: number           // 最近使った順に並べる
}

export type DayLog = {
  date: string                 // YYYY-MM-DD
  wakeAt: string | null        // HH:mm 起床時刻
  gotLight: boolean            // 起床後 高照度光 浴びたか
}

export type Store = {
  tasks: Task[]
  logs: DayLog[]
  presets: Preset[]
}
```

localStorage キー: `task-app-v1`。単一キーに `Store` を JSON 格納。
`presets` を持たない古いデータでも起動できるよう、`load()` で 無い配列は空に埋める。

## 3. 画面

全体で **1画面 + オーバーレイ**。画面遷移 なし。遷移 = 摩擦。

### NOW（メイン、常時これ）

`NOW` は画面の呼び名。ヘッダー左上に出すのは アプリ名 **やるぞ！**。

- 表示: **タスク1件のみ**。刺激度 + タイトル + 経過時間
- リスト全体 非表示（全体量が見えると凍る）
- `Space` = タイマー開始/一時停止。止めても経過は消えない（再開で続く）
- `Enter` = 完了 → 次を引く
- `P` = いったんやめる。**回数制限なし**。逃げ道は塞がない
- 下端に小さく 累積カウンタ（例: `今月 47`）
- 起動直後から既にタスク表示済み。入力フォーム 先に見せない

タスク選択ロジック（通常時）:
1. 未完了 かつ 子タスクなし が候補
2. `P` でやめた分を 候補から除く（`withoutPassed`）
3. `createdAt` 昇順（古い順）の **先頭 1 件**
4. ※ 重要度・締切ソート **なし**。抽選も **なし**

引くのは **イベントのときだけ**: 起動 / 完了 / `P` / 眠気切替。
表示のたびに引くと 画面が勝手に入れ替わる。引いた札は `pickedId` で持つ。

`P`（いったんやめる）:

- 外した札を **溜める**。押すほど 候補が減る → 押すたび 違うものが出る
- 全部やめた → 溜めを捨てて 最初から。詰ませない（`withoutPassed`）
- 溜めは **永続化しない**。閉じれば戻る。
  残すと「避けているタスク一覧」になる = 恥の生成装置（4章）

**`R`（引き直し）は やめた。** 抽選を外したので「外して引き直す」= 次に古いものを出す だけになり、
もう一度押すと 元に戻って 往復する。`P` の下位互換。送る手は 1 つでいい。

眠気モードのときは 刺激度 降順。覚醒を戻すのが先。

### 追加（オーバーレイ）

- `N` キー or 下端「＋」
- **定番は 1タップ**。押すだけで生える。入力ゼロ（1.5章）
- 定番の登録は「定番にも入れる」ボタン 1 つ。入れると同時に 雛形が残る
- 定番の削除は 各定番の右の ×。確認は挟まない
- 自由入力は 1行だけ
- 見積分・刺激度は デフォルト `estimateMin: 15, stimulation: 2`
- 入力項目を これ以上 増やすな。増やす = 管理作業

### 分解（オーバーレイ）

- NOW 画面で `D`
- 3つの問いを固定で出し、自分で埋める → `parentId` 付き子タスク生成
  1. 最初の5分で できることは？
  2. 次は？
  3. 終わりの目印は？
- 埋めた分だけ子になる（1個でも可）。見積は親の等分（最低5分）、刺激度は親を継ぐ
- 親タスクは 子が全完了で自動完了。カウントは葉のみ（親を二重に数えない）
- 3ステップ固定。数を可変にするな（選択 = 摩擦）
- LLM 呼び出しは**採用しない**。Claude Pro に API 利用は含まれず別課金になるため。
  「3つに割る」という構造自体が凍結を解くので、埋めるのは人間でいい。
  将来入れるなら「LLM に下書きさせる」ボタンの追加であって、この手動フローの置き換えではない

### 眠い（オーバーレイ）

- NOW 画面で `S`
- 動作: タスク選択ロジックを **`stimulation` 降順** に切替
- 該当タスクなし → 5分の身体タスクを提示（歩く/水飲む/顔洗う）
- 切替は当日限り。翌日 通常に戻る

### 今日（オーバーレイ）

- `T`
- 本日の完了タスク一覧 + 件数
- 起床時刻入力 + 「光 浴びた」トグル（DayLog 更新）
- **連続日数 表示しない**

## 4. 報酬設計

- 完了時: 短い視覚フィードバック。内容を **ランダムに数種から選択**（新規性減衰対策）
- カウンタは **累積のみ**（`今月 N` / `通算 N`）
- 未達・空白日を **一切表示しない**。カレンダーの穴 = 恥の生成装置
- 通知・催促 なし。催促は罪悪感 生む

### 名言（引用）

出す場所は **2つだけ**。データは `lib/quotes.ts`。

- **からっぽ画面**（タスク 0 件）→ `REST_QUOTES`
  - 何もしていない状態を **肯定する** 言葉のみ。足るを知る・無用の用・休息
  - 奮起・努力・継続・意志 の言葉 **禁止**。ここで急かす = 催促 = 罪悪感
  - からっぽに入るたび 引き直す
- **完了 直後**（既存の一言に添える）→ `DONE_QUOTES`
  - 終えた瞬間なら 催促にならない。**積み上がった側** の言葉のみ
  - 「怠けるな」「意志が足りない」系 禁止（1章: 意志の問題ではない）

**出典が特定できるものだけ入れる。** ネットの名言集は誤帰属だらけ
（「エジソンの 99%」「アインシュタインの〜」の類）。気の利いた文でも 帰属 不明なら 却下。
孫引きの引用サイトは 出典にしない。原典に当たる。

- 古典 → 章まで。漢籍・日本古典
- 近代の人物 → 著書・書簡・演説の 一次資料まで
- **ゲーム・アニメのセリフ** → 着火には 一番効く（1章 INCUP の 興味・新規性 に直撃）。
  ただし 2つ 条件:
  - 文言は **手元の作品で確認してから** 貼る。一次資料がネットに無い領域。
    記憶と引用サイトは 当てにしない
  - 著作権 生きている。private・個人利用の前提。**public にするときは 見直す**

**画面に出すのは 現代語だけ。** 漢文の書き下しも 古文も 英語も 出さない。
読めない文字列 = 摩擦。意味が一瞬で入らないなら 名言の意味がない。
原文は `lib/quotes.ts` の各項の直上に コメントで残す（訳を直すときの根拠）。

振り分けは 場所で決める。熱い言葉は 完了直後（DONE）へ。からっぽ（REST）に
「諦めるな」を置かない。そこは 何もしていない状態を 肯定する場所。

## 5. フェーズ

上から順。各フェーズ単体で動く。

- **P1**: NOW画面 + 追加 + 完了 + タイマー + localStorage 永続化
- **P2**: 分解ボタン（手動 3 分割。LLM 呼出は不採用）
- **P3**: 眠いボタン + 起床/光 ログ
- **P4**: 累積カウンタ + ランダム報酬演出
- **P5**: 定番 1タップ + カウントアップ化 + いったんやめる `P` + PWA 化

P1 が動かないうちに P2 以降 着手 禁止。

## 6. ファイル構成

```
app/page.tsx        # NOW + オーバーレイ全部
lib/types.ts        # 型定義
lib/store.ts        # localStorage 読み書き 集約
lib/select.ts       # 次タスク選択ロジック（古い順 / 眠気 / やめた分の除外）
lib/quotes.ts       # 名言データ（からっぽ用 / 完了用）
```

`app/page.tsx` のイベントハンドラを `useCallback` で包むな。React Compiler が
「既存のメモ化を保持できない」と判定して **コンパイル全体を飛ばす**（lint error）。
自動メモ化に任せる。keydown の `useEffect` は依存配列なし = 毎 render 貼り替えでいい。

コンポーネント分割は 300行超えてから。先に切るな。

## 7. 作らない

明示的に スコープ外。要求されても却下。

- 優先度・重要度ソート、アイゼンハワーマトリクス
- プロジェクト階層、タグ、ラベル、フィルタ
  - **定番は例外ではない。** 定番が持つのは タイトルだけ・分類しない・絞り込めない から通っている。
    「定番にタグを付ける UI」を作った時点で これはタグ管理になる。作るな
- 1日の時間割、曜日固定、「今日は◯分やる」目標
- カレンダー同期、外部ツール連携
- 連続ストリーク、達成率グラフ、カレンダーヒートマップ
- 分類ごとの達成率・配分グラフ（「今週 0件」を見せる = 恥の生成装置）
- 配分の比率を表す数字（持つと目標になる。持たない）
- 通知・リマインダー催促
- 認証、マルチデバイス同期、共有
- 設定画面（設定 = 先延ばしの遊び場）

## 8. 未決

- ~~LLM プロバイダとキー管理~~ → 決着。LLM は使わない（上の分解を参照）
- body doubling 相当をどうするか。他人が要る機能 → localStorage 前提と衝突。P4 以降で再検討
