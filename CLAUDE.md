# CLAUDE.md

原始人口調。敬語不要。

## 何

**やるぞ！**（リポジトリ `yaruzo`）。ADHD 当事者用。目的は「タスク管理」でなく **着火**。
リスト管理アプリ ではない。**起動装置**。
名前 = 起動の合図そのもの。「やるぞ！」と思った瞬間に 立ち上げる。

設計根拠と全画面仕様 → [DESIGN.md](DESIGN.md)。実装前 必読。
環境構築・別 PC への引き継ぎ → [SETUP.md](SETUP.md)。

## 前提（曲げるな）

- ADHD脳 = 興味ベース神経系。**重要度では発火しない**。発火条件 INCUP（興味/新規/挑戦/緊急/情熱）
- → 優先度マトリクス・重要度ソート は原理的に無効。実装 禁止
- 配分を計画すると未達が生まれる → 時間割 禁止。**比率の数字を持たない**。分類軸も持たない
- 眠気 = 低覚醒。刺激不足で覚醒崩落。「眠いからできない」でなく「退屈だから眠い」。因果 逆
- ストリーク切れ → 恥 → アプリ放棄。**連続型ストリーク 禁止**。累積型のみ
- 新規性減衰は仕様。2日で飽きる前提で設計

## スタック

- Next.js (App Router) + TypeScript
- 永続化: localStorage のみ。サーバ・DB・認証 なし
- スタイル: Tailwind

## 状態

P1〜P5 実装済み。
P1 NOW・追加・完了・タイマー・永続化 / P2 分解（手動 3 分割）/
P3 眠い・起床/光ログ / P4 累積カウンタ・完了時のランダム一言 /
P5 定番（何度もやることの雛形。1タップで生える）・タイマーはカウントアップ /
名言（からっぽ画面 と 完了直後。lib/quotes.ts）/
いったんやめる `P`（外した分が積み上がる。永続化しない）/
目安分の変更（NOW のバッジを上下ドラッグ。5分刻み・5〜180分。上下キーでも可）/
PWA（app/manifest.ts・public/sw.js。インストールすればサーバなしで起動）。
外部サービスは使わない。LLM 分解は不採用（DESIGN.md 3章 分解）。

## コマンド

セットアップ後:

```
npm run dev     # 開発サーバ
npm run build   # ビルド
npm run lint    # lint
npm test        # テスト（node --test）
```

## git

作業者 1人。レビューする相手 いない。

- **コミット・push は 都度 確認しない。** 作業が通ったら そのまま `main` に push まで やる
- ブランチ 切らない。`main` 直。PR 不要
- push 前に通すもの: `npm test` → `npm run lint` → `npm run build` → `npx tsc --noEmit`
  （`tsc` は build の後。理由は SETUP.md 3章）
- 落ちたら push しない。落ちたと言う

## 実装方針

- 最小構成優先。ライブラリ追加は「数行で書けない」と確認してから
- 状態管理ライブラリ 不要。useState + localStorage で足りる
- localStorage アクセスは `lib/store.ts` 1ファイルに集約。コンポーネント直叩き 禁止
- `app/page.tsx` のハンドラを `useCallback` で包むな。React Compiler が
  「既存のメモ化を保持できない」で **コンパイル全体を飛ばす**（lint error）。自動メモ化に任せる
- SSR 中 `window` 参照 不可。`useEffect` 内 or `typeof window` ガード
- 型は `any` 禁止。データ形状は `lib/types.ts` 1箇所で定義
- 認証・マルチデバイス同期・チーム機能 スコープ外

## 機能追加 判定

追加前 2問:

1. これ **着火** 助けるか、**管理作業** 増やすか → 管理側なら却下
2. 罪悪感 生むか → 生むなら却下

## 用語

- タスク: 1件の やること
- 完了: 済み状態。削除ではない（達成感 可視化 のため履歴 保持）
- 刺激度: タスクの興味・新規性の強さ。眠気モードの並べ替えキー。**画面には出さない**（内部値）
- 定番: 何度もやることの雛形。追加画面で 1タップ → タスクが生える。持つのは タイトルだけ
- やめる: `P`。いま出ているタスクを候補から外す。外した分は溜まる。永続化しない
